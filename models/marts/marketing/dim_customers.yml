version: 2

models:
  - name: dim_customers
    description: Customer dimension containing data from marketing.
    columns:
      - name: customer_first_name
        description: Customer's given name
      - name: customer_last_name
        description: Customer's surname